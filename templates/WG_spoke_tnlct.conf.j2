[Interface]
{# lookup 自动去控制器本地 keys/组名/ 目录下读取私钥 #}
PrivateKey = {{ lookup('ansible.builtin.file', './keys/' + group_names[0] + '/private.key') }}
Address = {{ ISP1_tunnel_self }}/24
{# 路由交给 BIRD/内核策略路由管理,wg-quick 不写主表,避免两条隧道 AllowedIPs 冲突 #}
Table = off

[Peer]
{# 连接 Hub01 = GW11 (ct / ISP1),Peer 处填对端 hub 的公钥 #}
PublicKey = {{ lookup('ansible.builtin.file', './keys/hub1/public.key') }}
Endpoint = {{ WG_server_ISP1 }}:51820
AllowedIPs = 0.0.0.0/0, ::/0
PersistentKeepalive = 8



